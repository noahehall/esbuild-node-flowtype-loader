{
  "name": "esbuild-node-flowtype-loader",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build:bin": "node --experimental-loader=./loader.mjs ./node_modules/.bin/esbuild --bundle index.mjs --outfile=index.js"
  },
  "devDependencies": {
    "esbuild": "^0.14.23"
  }
}
